# Create a list of restaurant ratings (e.g., [4.2, 3.8, 4.9, 2.5, 4.0]) and use the sorted() function to display the ratings in descending order.
list = [4.2, 3.8, 4.9, 2.5, 4.0]
a = sorted(list,reverse=True)
print(a)
